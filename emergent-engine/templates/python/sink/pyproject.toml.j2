[project]
name = "{{ name }}"
version = "0.1.0"
description = "{{ description }}"
requires-python = ">=3.12"

dependencies = [
    "emergent-client>=0.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
