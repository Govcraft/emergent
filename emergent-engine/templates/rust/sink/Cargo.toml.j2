[package]
name = "{{ name }}"
description = "{{ description }}"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "{{ name }}"
path = "src/main.rs"

[dependencies]
emergent-client = "{{ sdk_requirement }}"
tokio = { version = "1", features = ["full"] }
